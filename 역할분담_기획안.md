# 개인 일정 및 과제 관리 프로그램 기획안

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 개인 일정 및 과제 관리 프로그램 |
| 개발 목적 | 사용자가 개인 일정, 과제, 시험, 약속 등을 등록하고 목록 조회, 수정, 삭제, 검색, 통계 확인을 할 수 있는 Windows 데스크톱 프로그램을 개발한다. |
| 개발 환경 | C#, Windows Forms, .NET, SQLite, GitHub |
| 작업 방식 | GitHub feature 브랜치 기반 작업 및 Pull Request 병합 방식 |
| 저장 방식 | SQLite 데이터베이스 파일 `schedule.db`를 사용하여 일정 데이터를 저장한다. |

이 프로젝트는 WinForms에서 Form 단위로 화면을 분리하고, SQLite DB 기능은 `DatabaseHelper` 클래스로 분리하여 저장 관련 역할 충돌을 줄이는 방식으로 진행한다.

## 2. 프로젝트 필요성

일상생활에서는 과제, 시험, 팀 프로젝트, 개인 약속 등 관리해야 할 일정이 많다. 이를 수기로 관리하거나 메신저, 메모장 등에 흩어져 관리하면 마감일을 놓치거나 우선순위를 파악하기 어렵다.

따라서 본 프로그램은 사용자가 일정을 한 곳에 등록하고, 마감일, 중요도, 카테고리, 완료 여부를 관리할 수 있도록 하는 것을 목표로 한다.

## 3. 주요 기능

| 기능 | 설명 | 담당자 |
|---|---|---|
| 일정 등록 | 제목, 내용, 마감일, 카테고리, 중요도를 입력하여 새 일정을 DB에 저장한다. | 성인 |
| 일정 목록 | 등록된 전체 일정을 표 형태로 조회하고, 오늘 일정 및 날짜별 일정을 확인한다. | 범준 |
| 일정 관리 | 선택한 일정을 수정, 삭제하거나 완료 상태로 변경한다. | 윤서 |
| 검색 | 제목, 내용, 카테고리, 중요도 등을 기준으로 일정을 검색한다. | 윤서 |
| 통계 | 전체 일정 수, 완료 일정 수, 미완료 일정 수, 카테고리별 개수를 표시한다. | 윤서 |
| DB 관리 | SQLite 연결, 테이블 생성, CRUD 메서드, 검색 및 통계 메서드를 구현한다. | 정영 |
| 메인 화면 및 통합 | 메인 화면 버튼 구성, 각 기능 화면 연결, GitHub 병합 및 최종 테스트를 담당한다. | 준서 |

## 4. 화면 구성

메인 화면은 다음 버튼을 중심으로 구성한다.

| 메인 버튼 | 연결 Form | 담당자 | 기능 요약 |
|---|---|---|---|
| 일정 등록 | `FormAddTask` | 성인 | 새 일정 추가 |
| 일정 목록 | `FormTaskList` | 범준 | 일정 조회 및 날짜별 확인 |
| 일정 관리 | `FormManageTask` | 윤서 | 수정, 삭제, 완료 처리 |
| 검색 | `FormSearch` | 윤서 | 일정 검색 |
| 통계 | `FormStats` | 윤서 | 완료율 및 개수 통계 |
| 종료 | 없음 | 준서 | 프로그램 종료 |

## 5. 담당자별 상세 역할

### 5.1 준서 - 메인 화면, GitHub 관리, 최종 통합

| 항목 | 내용 |
|---|---|
| 담당 파일 | `FormMain.cs`, `FormMain.Designer.cs`, `Program.cs`, `README.md` |
| 담당 브랜치 | `feature/main-ui` |

주요 작업:

- 메인 화면 UI 구성
- 일정 등록, 일정 목록, 일정 관리, 검색, 통계, 종료 버튼 배치
- 팀원들이 만든 Form을 메인 버튼과 연결
- `Program.cs`에서 `DatabaseHelper.InitializeDatabase()` 연결 확인
- GitHub 저장소 관리, PR 확인 및 `main` 병합
- 최종 실행 테스트 및 README 작성

### 5.2 정영 - DB 담당

| 항목 | 내용 |
|---|---|
| 담당 파일 | `Data/DatabaseHelper.cs`, `ScheduleProject.csproj` |
| 참고 파일 | `Models/TaskItem.cs` |
| 담당 브랜치 | `feature/database` |

주요 작업:

- `Microsoft.Data.Sqlite` NuGet 패키지 설치
- SQLite DB 파일 `schedule.db` 연결
- `Tasks` 테이블 자동 생성
- `InitializeDatabase()`, `AddTask()`, `GetAllTasks()` 구현
- `GetTaskById()`, `UpdateTask()`, `DeleteTask()` 구현
- `SearchTasks()`, `GetTotalTaskCount()`, `GetCompletedTaskCount()`, `GetCategoryCount()` 구현
- 다른 팀원이 SQL을 직접 작성하지 않고 `DatabaseHelper` 메서드만 사용하도록 공통 기능 제공

현재 DB 관련 구현은 `main`에 병합되어 있다. 각자 작업 전 `main`을 최신화해야 한다.

### 5.3 성인 - 일정 등록 담당

| 항목 | 내용 |
|---|---|
| 담당 화면 | `FormAddTask` |
| 담당 파일 | `Forms/FormAddTask.cs`, `Forms/FormAddTask.Designer.cs`, `Forms/FormAddTask.resx` |
| 담당 브랜치 | `feature/add-task` |

주요 작업:

- 일정 제목 입력 `TextBox` 구성
- 일정 내용 입력 `TextBox` 구성
- 마감일 선택 `DateTimePicker` 또는 `MonthCalendar` 구성
- 카테고리 선택 `ComboBox` 구성
- 중요도 선택 `ComboBox` 구성
- 등록 버튼 클릭 시 입력값 검증
- `TaskItem` 객체 생성 후 `DatabaseHelper.AddTask(task)` 호출
- 등록 완료 메시지 출력 및 창 닫기

### 5.4 범준 - 일정 목록 담당

| 항목 | 내용 |
|---|---|
| 담당 화면 | `FormTaskList` |
| 담당 파일 | `Forms/FormTaskList.cs`, `Forms/FormTaskList.Designer.cs`, `Forms/FormTaskList.resx` |
| 담당 브랜치 | `feature/task-list` |

주요 작업:

- `DataGridView`를 사용하여 일정 목록 출력
- `DatabaseHelper.GetAllTasks()`를 사용하여 전체 일정 조회
- 새로고침 버튼 구현
- 오늘 일정 보기 기능 구현
- 날짜 선택 시 해당 날짜 일정만 조회
- 이번 주 일정 보기 기능 구현
- 완료 여부를 목록에서 확인할 수 있도록 표시

날짜별 필터링은 우선 `GetAllTasks()`로 전체 목록을 가져온 뒤 `DueDate.Date` 기준으로 필터링한다.

### 5.5 윤서 - 일정 관리, 검색, 통계 담당

| 항목 | 내용 |
|---|---|
| 담당 화면 | `FormManageTask`, `FormSearch`, `FormStats` |
| 담당 파일 | `Forms/FormManageTask.cs`, `Forms/FormSearch.cs`, `Forms/FormStats.cs` |
| 담당 브랜치 | `feature/manage-search-stats` |

주요 작업:

- 일정 관리 화면에서 선택한 일정 수정, 삭제, 완료 처리 구현
- `DatabaseHelper.GetTaskById(id)`, `UpdateTask(task)`, `DeleteTask(id)` 사용
- 검색 화면에서 키워드 검색 및 결과 출력 구현
- `DatabaseHelper.SearchTasks(keyword)` 사용
- 통계 화면에서 전체 일정 수, 완료 일정 수, 미완료 일정 수, 카테고리별 개수 표시
- `DatabaseHelper.GetTotalTaskCount()`, `GetCompletedTaskCount()`, `GetCategoryCount()` 사용

현재 `SearchTasks(keyword)`는 제목과 내용 기준 검색만 지원한다. 카테고리와 중요도까지 검색하려면 DB 메서드 확장이 필요하다.

## 6. 데이터베이스 설계

SQLite를 사용하며, 일정 데이터는 `Tasks` 테이블에 저장한다.

| 컬럼명 | 자료형 | 설명 |
|---|---|---|
| `Id` | `INTEGER PRIMARY KEY AUTOINCREMENT` | 일정 고유 번호 |
| `Title` | `TEXT NOT NULL` | 일정 제목 |
| `Description` | `TEXT` | 일정 내용 |
| `DueDate` | `TEXT NOT NULL` | 마감일 |
| `Category` | `TEXT` | 과제, 시험, 약속, 기타 등 카테고리 |
| `Priority` | `TEXT` | 높음, 보통, 낮음 등 중요도 |
| `IsCompleted` | `INTEGER` | 완료 여부. `0`은 미완료, `1`은 완료 |
| `CreatedAt` | `TEXT` | 일정 등록일 |

## 7. GitHub 작업 계획

| 브랜치 | 담당자 | 작업 내용 |
|---|---|---|
| `main` | 준서 | 최종 안정 버전 관리 |
| `feature/main-ui` | 준서 | 메인 화면, 버튼 연결, `Program.cs`, README |
| `feature/database` | 정영 | SQLite DB 연결 및 `DatabaseHelper` 구현 |
| `feature/add-task` | 성인 | 일정 등록 화면 및 DB 저장 기능 |
| `feature/task-list` | 범준 | 일정 목록 화면 및 날짜별 조회 기능 |
| `feature/manage-search-stats` | 윤서 | 일정 관리, 검색, 통계 화면 구현 |

작업 규칙:

- `main` 브랜치에서 직접 작업하지 않는다.
- 각자 담당 feature 브랜치에서 작업한다.
- 작업 완료 후 Pull Request를 통해 병합한다.
- WinForms Designer 충돌을 줄이기 위해 각자 담당 Form만 수정한다.

## 8. Git 작업 규칙

작업 시작 전:

```bash
git checkout main
git pull origin main
git checkout 본인브랜치
git merge main
```

작업 완료 후:

```bash
git status
git add .
git commit -m "[Add] 작업 내용"
git push origin 본인브랜치
```

브랜치 생성 예시:

```bash
git checkout main
git pull origin main
git checkout -b feature/add-task
git push -u origin feature/add-task
```

## 9. 개발 일정 계획

| 단계 | 작업 내용 | 담당자 |
|---|---|---|
| 1단계 | 프로젝트 생성, GitHub 저장소 생성, 메인 화면 기본 구성 | 준서 |
| 2단계 | SQLite 패키지 설치, `DatabaseHelper` 구현, DB 기본 구조 완성 | 정영 |
| 3단계 | 일정 등록 화면 구현 및 DB 저장 연결 | 성인 |
| 4단계 | 일정 목록 화면 구현 및 DB 조회 연결 | 범준 |
| 5단계 | 일정 관리, 검색, 통계 화면 구현 | 윤서 |
| 6단계 | 메인 화면과 전체 Form 연결, 통합 테스트 | 준서 및 전체 팀원 |
| 7단계 | 오류 수정, 발표 자료 및 시연 준비 | 전체 팀원 |

## 10. 예상 완성 결과

프로그램을 실행하면 메인 화면이 표시되고, 사용자는 일정 등록, 일정 목록, 일정 관리, 검색, 통계 기능을 선택할 수 있다.

등록된 일정은 SQLite DB에 저장되며, 프로그램을 종료했다가 다시 실행해도 데이터가 유지된다. 목록 화면에서는 저장된 일정을 표 형태로 확인할 수 있고, 관리 화면에서는 수정, 삭제, 완료 처리가 가능하다. 검색 화면과 통계 화면을 통해 사용자는 자신의 일정 상태를 쉽게 파악할 수 있다.

## 11. 기대 효과

- 개인 일정과 과제를 체계적으로 관리할 수 있다.
- 마감일, 중요도, 완료 여부를 한눈에 확인할 수 있다.
- WinForms의 Form, Button, TextBox, ComboBox, DataGridView 등 주요 컨트롤을 활용할 수 있다.
- SQLite를 이용한 데이터 저장 및 CRUD 기능을 실습할 수 있다.
- GitHub 브랜치와 Pull Request를 사용하는 팀 작업 방식을 경험할 수 있다.

## 12. 역할 분담 요약

| 이름 | 역할 | 핵심 책임 |
|---|---|---|
| 준서 | 메인 화면, GitHub 관리, 최종 통합 | 프로젝트 통합, 메인 화면, PR 관리 |
| 정영 | DB 담당 | SQLite 연결, `DatabaseHelper` 구현 |
| 성인 | 일정 등록 | 일정 입력 UI 및 DB 저장 |
| 범준 | 일정 목록 | 전체/날짜별 일정 조회 |
| 윤서 | 일정 관리, 검색, 통계 | 수정, 삭제, 완료, 검색, 통계 |

